import makeStyles from "../../utils/makeStyles";

const useMapTableStyles = makeStyles({
  root: {
    width: '100%',
    minHeight: '100vh',
    background: 'black',
  },
});

export default useMapTableStyles;
